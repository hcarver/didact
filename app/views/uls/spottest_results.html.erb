<%
@right = @results[:right]
@extra = @results[:wrong]
@missed = @data.select{|item| @results[:right].select{|other| @item.soft_equal(other, item) }.length == 0}
%>

<h3><%=@right.length%> out of <%=@data.length%>:</h3>

<%= render 'results_display' %>

<%= link_to 'Try these again', @retry_url %>
| 
<%= link_to 'Move on', @progress_url %>
<br/>
<br/>
<%= link_to 'Back to menu', :root %>
